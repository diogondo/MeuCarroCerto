@model MeuCarroCerto.Models.CriteriosPessoa
@{
    ViewBag.Title = "Index";
}
<h2>Pondere seus critérios</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <div class="editor-label">
            @Html.Label("Necessidade")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.necessidade, (SelectList)ViewBag.criterio, "Importância")
        </div>

        <div class="editor-label">
            @Html.Label("Qualidade")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.qualidade, (SelectList)ViewBag.criterio, "Importância")
        </div>

        <div class="editor-label">
            @Html.Label("Segurança")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.segurança, (SelectList)ViewBag.criterio, "Importância")
        </div>

        <div class="editor-label">
            @Html.Label("Valor")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.valor, (SelectList)ViewBag.criterio, "Importância")
        </div>

        <div class="editor-label">
            @Html.Label("Marketing")
        </div>
        <div class="editor-field">
            @Html.DropDownListFor(model => model.marketing, (SelectList)ViewBag.criterio, "Importância")
        </div>



        <p>
            <input type="submit" value="Continuar" />
        </p>
    </fieldset>
}